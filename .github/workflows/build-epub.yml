name: Build EPUB

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build EPUB
        run: |
          chmod +x scripts/epub.sh
          ./scripts/epub.sh

      - name: Upload EPUB artifact
        uses: actions/upload-artifact@v4
        with:
          name: ebook
          path: public/ebook.epub
